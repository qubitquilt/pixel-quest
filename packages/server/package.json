{
  "name": "server",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm --filter shared build && pnpm exec tsc --build",
    "test": "jest",
    "dev": "pnpm exec ts-node index.ts",
    "start": "pnpm exec node dist/index.js",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "@colyseus/testing": "^0.16.3",
    "@types/express": "^5.0.3",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.17.9",
    "@types/ws": "^8.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.60",
    "@colyseus/ws-transport": "^0.16.5",
    "colyseus": "^0.16.4",
    "express": "^5.1.0",
    "reflect-metadata": "^0.2.2",
    "shared": "workspace:*"
  }
}
