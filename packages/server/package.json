{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "dev": "ts-node --project ../../tsconfig.json -r tsconfig-paths/register index.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.3"
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.60",
    "colyseus": "^0.16.4",
    "express": "^5.1.0",
    "reflect-metadata": "^0.2.2",
    "shared": "*"
  }
}
